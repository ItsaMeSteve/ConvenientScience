import 'package:flutter/material.dart';

class AboutPage extends StatelessWidget {
  static const String id = 'insert_page_navigator_name';

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Things stuff n whatnot'),
    );
  }
}
